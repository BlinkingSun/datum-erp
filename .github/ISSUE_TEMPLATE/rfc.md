---
name: Request for comment
about: Required before changing a public surface
title: 'RFC: '
labels: 'type:rfc'
assignees: ''
---

<!-- REQUIRED before a pull request that touches any of:
       a public HTTP path, an event payload, the manifest schema, the module
       contract or hook interface, the canonical module order, or the workspace
       dependency allowlist.

     If the decision would be expensive to REVERSE rather than merely public,
     it is an architecture decision record instead. See docs/adr/README.md. -->

## Surface affected

<!-- Name the files. -->

## The change

## Why the current shape is wrong

## What it breaks

<!-- Below 1.0 there is no compatibility promise, but breakage is still stated
     plainly, not discovered by a contributor later. -->

## Goal alignment

<!-- Which of the four goals in GOALS.md this serves, and which it strains.
     A change that serves one goal by weakening another is not automatically
     rejected, but the trade is stated here rather than buried. -->

## Alternatives

<!-- What else was considered and why it lost. -->
